# 实验记录

本文档记录 SFT + DPO 训练过程中的环境、配置、结果、踩坑，以及求职展示的改进建议。

## 环境

- Python 3.12 + 虚拟环境 `D:\sft_env`
- PyTorch 2.5.1+cu121 | CUDA 12.1 | RTX 3060 (12GB)
- transformers 5.17.0 | peft 0.21.0 | datasets 5.0.1

## 数据

- 原始 1047 条 → MinHash 去重 448 条 → 训练 358 / 验证 44 / 测试 46
- 详细统计见 `data/processed/data_report.txt`

## Stage 2: SFT 训练配置与结果

| 参数 | 值 |
|------|-----|
| 模型 | Qwen/Qwen1.5-1.8B |
| LoRA | r=16, alpha=32, dropout=0.05 |
| LR | 2e-4, cosine schedule, warmup 5% |
| Batch | 4×8 (effective 32) |
| Epochs | 5（早停 patience=3） |
| Max Len | 2048 |
| 优化器 | adamw_torch + fp16 + grad checkpoint |

| 指标 | 值 |
|------|-----|
| 训练 loss | 1.829 → 0.165 |
| 验证 loss | 0.587 → 0.230 |
| 最终 PPL | **1.26** |
| 训练时长 | 354s |
| 可训练参数 | 14,991,360 (0.81%) |

## Stage 3: DPO 训练配置与结果

| 参数 | 值 |
|------|-----|
| beta | 0.1 |
| LR | 5e-5, cosine |
| Batch | 1×16 |
| 精度 | bf16 + max_grad_norm=1.0 |
| reference | disable_adapter() 零额外显存 |

| 指标 | 值 |
|------|-----|
| 训练 loss | 0.678 → 0.010（初始 ≈ ln2） |
| 验证 loss | 0.059 → 0.034 |
| 训练时长 | 278s |

## 踩坑记录

### 1. FlashAttention2 不可用
transformers 5.x 默认尝试 flash_attn，但 Windows 极难编译。
**解决**：用 `AutoConfig` 显式设置 `config._attn_implementation = "sdpa"`。

### 2. transformers 5.17.0 API 变更
- `warmup_ratio` → 已移除，用 `warmup_steps`（支持 0-1 浮点数作比例）
- `logging_dir` → 已移除，TensorBoard 日志由 `output_dir` 自动管理
- `report_to="tensorboard"` → 需要装 tensorboard 包，改成 `"none"` 跳过

### 3. 模型下载慢
**解决**：`pip install hf_transfer` + `HF_ENDPOINT=https://hf-mirror.com`

### 4. TRL 与 torch 2.5.1 不兼容
新版 TRL 依赖 `FSDPModule`（torch 2.6+）。
**解决**：放弃 TRL，手写 DPO 损失（基于 transformers Trainer）。

### 5. fp16 下 DPO 梯度 nan
loss 骤降导致数值溢出。
**解决**：改 bf16（RTX 3060 Ampere 支持）+ 梯度裁剪 max_grad_norm=1.0。

### 6. DPO 显存优化
reference logps 用 `model.disable_adapter()` 在同一模型上计算，避免加载第二份模型。

### 7. ChatML 模板泄露
推理时需将 `<|im_end|>` 设为 eos token 并在解码后截断。

## 内存优化建议（可选）

- `MAX_LEN` 2048 → 1024（数据 99% < 300 字）
- `PER_DEVICE_BS` 4 → 2，`GRAD_ACC` 8 → 16
- `__getitem__` 返回 Python list 而非 tensor（避免 DataCollator 重复 tensor 化泄漏）

---

## 附录：求职展示改进建议

### 项目亮点
- 完整 LLM 训练链路（数据→SFT→DPO→推理），比多数应届生只调 API 强
- 真跑过 LoRA 微调，看得懂 PPL/loss，不是只会调包
- 解决过真实工程问题（CUDA/transformers 版本/显存）
- DPO 手写损失（不用 TRL），理解算法原理

### 待补强（按性价比排序）

1. **部署成在线 API + Demo**（最重要，1-2天）
   - FastAPI 封装推理接口 → Docker → 云服务器部署 → Gradio/网页 Demo
   - README 放可点击的在线 Demo 链接

2. **加定量评估**（1天）
   - 测试集 PPL 对比表（Base/SFT/DPO）
   - 20-50 个问题人工评分（正确性/完整性/格式，1-5 分）

3. **数据加入真实成分**（1-2天）
   - 从开源中文 QA 数据集抽样 500-1000 条混入
   - 讲清数据构建 pipeline

### 简历写法示例

> **Qwen-1.8B 技术问答模型全流程微调**
> - 独立完成 1000+ 条技术问答数据的采集/MinHash 去重/质量过滤，构建 SFT+DPO 两阶段训练数据
> - 基于 LoRA（r=16）完成 SFT 微调，PPL 从 1.80 降至 1.26；手写 DPO 损失完成偏好对齐
> - 使用 FastAPI + Docker 部署推理服务，支持流式输出
> - （附 GitHub 链接 + 在线 Demo）

### 结论

- 投中小厂 / 实习 / 算法岗：补完部署 + README 就够用
- 投大厂 LLM 相关岗位：四点全补，且要能讲清 DPO 公式推导、LoRA 原理、显存估算
- 投后端/数据岗：项目超配，重点讲部署和工程化